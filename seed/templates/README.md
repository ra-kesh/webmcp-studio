# Synthetic template seed

The executable Northstar Weddings seed lives in `packages/document/src/seed.ts` so schema validation runs at import time and tests use the same data as the Studio and Renderer apps.

Before public launch, add an asset manifest here with source URL, license, attribution, checksum, dimensions, and R2 key for every seeded image. Do not add private customer or studio data.
